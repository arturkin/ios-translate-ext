# macOS
.DS_Store

# Xcode — user-specific state
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout

# Build output
build/
build-mac/
chrome/dist/
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.dSYM
*.dSYM.zip

# Swift Package Manager
.build/
.swiftpm/

# Local secrets — never commit API keys (public repo!)
# The real env file with your key is gitignored; only the *.example template is tracked.
Config/Secrets.xcconfig
*.local.xcconfig
secrets.xcconfig
# Chrome: baked key lives here (gitignored); only chrome/config.example.js is tracked.
chrome/config.local.js
